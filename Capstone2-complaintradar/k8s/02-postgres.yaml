apiVersion: apps/v1
kind: Deployment
metadata:
  name: complaintradar-postgres
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: complaintradar-postgres
  template:
    metadata:
      labels:
        app: complaintradar-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: complaintradar_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: complaintradar-postgres
  namespace: default
spec:
  selector:
    app: complaintradar-postgres
  ports:
    - port: 5432
      targetPort: 5432
