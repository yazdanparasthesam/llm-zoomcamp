services:
  - type: web
    name: complaintradar
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: USE_DOCKER
        value: "false"
      - key: GROQ_API_KEY
        sync: false
